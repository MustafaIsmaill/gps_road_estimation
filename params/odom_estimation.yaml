
# topic name that has the path msg
path_topic: "route_points"

# topic name that has the odometry to be matched
pub_topic: "/ada/odometry_fusion_ekf"

# topic name to publish gps projected points odometry to 
pp_pub_topic: "/ada/odometry_fusion_projected"

